# 父组件的打字机样式覆盖它和它的 `extra` 子组件，共一条时间线

`typewriter` 写在父组件上时，父组件自己的文字与全部 `extra` 子组件连成一整段、按同一条时间线逐字打出（共用会话与逐字进度）；某个子组件若自带 `typewriter`，它就从父组件那条时间线里脱离、另起一条。于是两种常见写法都能表达：想"整句一起打"就写在父组件上，想"逐条依次出现"就给每条子组件各写一份。

反过来（默认每个子组件各自独立计时）表达不了"整句连打"：父组件的正文与各子组件会各自从第一个字开始同时往外吐，而不是依次推进成一句话。
