# 打字机文本样式（typewriter_text）

NeoForge 1.21.1 模组：把"逐字显示"做成原版文本组件样式上的一个字段，而不是新的组件类型。领域语言在此建档，代码与文档必须使用本文件的规范术语。原版概念（`Style`、`Component`、样式字段）按 Minecraft 官方叫法，不另起名字。

## Language

### 打字机

**打字机样式（`typewriter`）**:
组件样式上的一个可选字段，声明这段文字逐字显示；凡是能写 `color`、`bold` 的地方都能写它，文字内容仍由原版内容类型提供。
_Avoid_: 打字机组件、打字机内容类型、`typewriter` 组件

**出字速度（`time`）**:
相邻两个字符间隔多久，数值越小出字越快。数字按 tick 计（整数），也可写带单位的字符串：`"0.25s"`（秒，可小数）、`"4t"`（tick）；省略时每字符 2 tick。
_Avoid_: 总时长、时长、duration

**打字机身份**:
判等时参与比较的那部分打字机参数——出字速度与逐字指令。`session` 与播放进度不参与。
_Avoid_: 会话身份、参数指纹

**逐字进度**:
一段打字机文字当前已显示到第几个字符。由客户端在渲染时刻推进，服务端读到的永远是全文。
_Avoid_: 播放位置、动画帧、已打字数

**时间线**:
一个打字机样式实例从起笔（首次被读到）到收尾的整段计时，决定每个字符在什么时刻出现。
_Avoid_: 动画、播放、进度条

**会话（`session`）**:
随打字机样式一起序列化、双端一致的标识，用来把客户端发回的字符回报对回服务端登记的逐字指令。省略时自动生成。
_Avoid_: 播放 id、token、会话编号

**字符回报（CharTyped）**:
客户端发往服务端的一条"第几个字符已经打出"的上报，只含会话、序号、总字数与该字符本身，不含任何指令文本。
_Avoid_: 打字事件、回调、回执

**逐字指令（`command`）**:
每打出一个字符时由服务端静默执行一次的指令文本，占位符 `%c`/`%i`/`%n`。指令文本只保留在服务端登记表里，永远不下发客户端。
_Avoid_: 打字音效指令、触发器、钩子

**收尾**:
让一段打字机文字停止逐字、立刻显示全文（自然打完，或被 `/tw stop` 提前收尾）。
_Avoid_: 结束、停止播放、补完

### 显示

**文本通道**:
任何"把文本组件变成可绘制文字"的原版出口，聊天栏、标题、动作栏、bossbar、物品名、工具提示都算一条通道。打字机样式对全部通道同时生效。
_Avoid_: 显示位置、渲染目标、渲染器

**样式继承**:
父组件的打字机样式对它自己和它的 `extra` 子组件生效，整段共用一条时间线；子组件自带打字机样式时，它从那一条时间线里脱离、另起一条。
_Avoid_: 传递、冒泡、级联

### 扭曲

**扭曲样式（`distort`）**:
组件样式上的独立字段，让文字在排版位置不变的前提下逐字符做位移形变；可与打字机样式各自单独用或叠加。
_Avoid_: 动画样式、振荡、抖动组件

**字形位移**:
单个字形在屏幕上额外加的横向/纵向偏移量（GUI 像素）。它不改变排版与换行，只在绘制那一刻生效。
_Avoid_: 字形偏移量、抖动值

**波浪（`wave`）**:
扭曲效果之一：位移按正弦沿文字方向传播，空间相位取自字符的像素横坐标，因此沿整行连续，不受组件被拆成几段影响。
_Avoid_: 摇摆、波动

**漂移（`jitter`）**:
扭曲效果之一：每个字在自己半径内缓慢游动，随机但平滑；随机相位由参数哈希与字符下标决定，同一段文字观感可复现。
_Avoid_: 抖动、随机晃动

**抖动（`shake`）**:
扭曲效果之一：高频、大幅度的折返位移，带"频闪"的毛躁感。
_Avoid_: 振动、颤抖

### 指令

**附加指令（`tw` / `typewriter`）**:
本模组额外注册的便捷指令 `tw show`、`tw stop`（`typewriter` 是 `tw` 的同义长名）。打字机样式本身不依赖它们，原版 `/tellraw`、`/title`、`/bossbar` 即可直接用。
_Avoid_: 打字机命令、tw 命令、扩展指令
